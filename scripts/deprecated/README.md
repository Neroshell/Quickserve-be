# Deprecated migration scripts

Scripts in this directory are retained only as historical evidence. They must
not be used for deployments. Use the explicitly named `audit:collections`,
`migrate:collections`, and `migrate:collections:rollback` package scripts.
